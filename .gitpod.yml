tasks:
  - init: npm install
    command: npm start

ports:
  - port: 3000
    onOpen: open-browser # saving files don't work inside iframe :(

vscode:
  extensions:
    - dbaeumer.vscode-eslint
    - editorconfig.editorconfig
    - esbenp.prettier-vscode
    - flowtype.flow-for-vscode
